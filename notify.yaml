  - name: Slack
    platform: slack
    api_key: !secret slack-bot-key
    default_channel: '#general'
  - platform: html5
    name: browser
    vapid_pub_key: !secret vapid_pub_key
    vapid_prv_key: !secret vapid_prv_key
    vapid_email: !secret vapid_email